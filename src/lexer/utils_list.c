#include "minishell.h"

//1- get the 'word'
//   when find the character that is not quotes and spaces, substracts it 
//2- initialize a node with it's properties
//3- add the node into a linked list 



t_token **token_add_new(t_token **token_list, char *input)
{
    
}